{
  "name": "veltrix-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "Local engineering control plane for repository analysis, workflow runs, task audit, and review preparation.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  }
}
